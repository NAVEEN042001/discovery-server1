apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: Build with Maven
        uses: docker://maven:3.8.1-openjdk-17
        run: |
          mvn clean install

